import { Typography } from '@mui/material';
import React from 'react';

const AddMeal = () => {
    return (
        <div>
            <Typography>Add a New Meal</Typography>
        </div>
    );
};

export default AddMeal;